import { ObjectChangeset } from '@riboseinc/paneron-extension-kit/types/objects';
import { ChangeRequest } from '../../types';

export function proposalsToObjectChangeset(cr: ChangeRequest["proposals"]): ObjectChangeset {
}
